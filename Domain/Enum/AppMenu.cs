using System;
namespace ATMApp.Domain.Enum
{
    public enum AppMenu
    {
        CheckBalance = 1,
        PlaceDeposit,
        MakeWithdrawal,
        InternalTransfer,
        VeiwTransaction,
        Logout,
    }
}
